Allow assessment email preview in production

- Drop the NODE_ENV production guard on /email-preview/assessment so
  the page renders on the live deployment for client review
- Add `robots: noindex, nofollow` metadata + disallow /email-preview/
  in robots.txt so search engines do not index the preview
- Tighten the on-page eyebrow copy ("Internal preview")
